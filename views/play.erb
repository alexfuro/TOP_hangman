<h1> Hello there: <%= name %>!</h1>
<img src="<%= misses - 1 %>.png" alt="hangman image">
<p>I'm thinking of a word that's <%= secret_word.length %> long:</p>
<p class="game_board"><%= game_board.join %></p>
<p>Your guesses were</p>
<p class="player_guesses"><%= guesses.join(" ") %></p>
<p>you have made: <%= misses - 1 %> misses</p>
<form>
  What is your guess?: <input type="text" name="guess" />
  <input type="submit" value="Guess!" />
</form>
<% if misses <= 8 && winner %>
  <h2>You won!</h2>
<% elsif misses > 8 && !winner %>
  <h2>You lose!</h2>
  <p>The word was <%= secret_word %> all along</p>
<% end %>
<a href="/">New game</a>
